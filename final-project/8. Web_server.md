# Web Server

**Requirements**
- NGINX/Apache2/Lightspeed on Gateway
- SSL Certbot using Wildcard
- Automatic SSL (Ansible/Cronjob/Script etc.)

**Instructions**
- Create domains:


# Setup Nginx dan SSL

Disini saya menggunakan IaC (Ansible) dalam setup/konfigurasi nginx menggunakan ssl.

## Setup SSL dan Cloudflare menggunakan Ansible

```
- name: "Install SSL Certbot using snap"
  snap:
    name: certbot
    classic: true
    state: present

- name: "Prepare certbot symlink"
  command: ln -s /snap/bin/certbot /usr/bin/certbot
  args:
    removes: /usr/bin/certbot  # Jika sudah ada tidak akan membuat baru

- name: "Trust cloudflare DN plugin"
  command: snap set certbot trust-plugin-with-root=ok

- name: "Install cloudflare DNS plugin"
  snap:
    name: certbot-dns-cloudflare
    state: present

- name: "Create directory secret"
  file:
    path: "/home/{{ new_user }}/.secret"
    state: directory
    mode: '0700'

- name: "Create cloudflare credentials file"
  template:
    src: ../../templates/certbot_cloudflare/cloudflare.ini.j2
    dest: "/home/{{ new_user }}/.secret/cloudflare.ini"

- name: "Run Certbot to generate certificates"
  command: >
    certbot certonly
    --dns-cloudflare
    --dns-cloudflare-credentials /home/{{ new_user }}/.secret/cloudflare.ini
    --agree-tos
    --non-interactive
    --email your-email
    --expand
    --domains *.your-domain.studentdumbways.my.id
    --domains your-domain.studentdumbways.my.id -v
  timeout: 60
```

![ssl1](https://github.com/user-attachments/assets/34363412-adad-4496-a84a-c20edc529512)


## Docker Compose untuk Nginx

```
services:
  nginx:
    container_name: nginx
    image: nginx:latest
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/conf.d/:/etc/nginx/conf.d/
      - /etc/letsencrypt/live/olen.studentdumbways.my.id/fullchain.pem:/etc/nginx/ssl/fullchain.pem
      - /etc/letsencrypt/live/olen.studentdumbways.my.id/privkey.pem:/etc/nginx/ssl/privkey.pem
```

## Setup Reverse Proxy

- Membuat directory/folder untuk menampung seluruh konfigurasi reverse proxy
  ![ssl2](https://github.com/user-attachments/assets/701fd9fa-e079-488d-ab2e-e46785a1b4c9)

- Masukkan domain yang ingin digunakan dan memiliki ssl certificate (Aplikasi berjalan pada port 8080)

```
# Reverse Proxy untuk Aplikasi
  server {
    listen 80;
    server_name your-domain.studentdumbways.my.id;
    return 301 https://$host$request_uri;
  }

  server {
    listen 443 ssl;
    server_name your-domain.studentdumbways.my.id;

    ssl_certificate /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;

    location / {
      proxy_pass http://your-ip:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }
```

## Automatic renewal SSL

- Membuat file Bash untuk SSL renewal
  ![renewal1](https://github.com/user-attachments/assets/e0a14d0d-77d4-4433-b9ce-bfdb7eea1fbc)

- Masukkan script berikut

```
#!/bin/bash

# Perbarui sertifikat SSL menggunakan cloudflare
sudo certbot renew --dns-cloudflare --dns-cloudflare-credentials /etc/letsencrypt/cloudflare.ini

# Reload nginx
docker compose restart nginx

echo "SSL certificates renewed and nginx reloaded"
```

- Berikan izin eksekusi pada file
  ![renewal3](https://github.com/user-attachments/assets/8315b2e7-c1cc-4e13-8e8f-e58047532f1d)


- Mengatur cronjob untuk automasi
```
sudo crontab -e
```

- Menambahkan script dibawah ini pada baris
```
0 0 1 * * /usr/local/bin/renewal-certificate >> /var/log/renewal-certificate.log 2>&1
```
  ![renewal5](https://github.com/user-attachments/assets/2bb0b47c-8c62-48aa-b609-7b0486ec2ba1)


## Penjelasan Script
- Menit (0): Eksekusi pada menit (tepat diawal jam).
- Jam (0): Eksekusi pada pukul 00.00 (tengah malam).
- Hari dalam bulan (1): Eksekusi pada hari pertama setiap bulan.
- Bulan (*): Eksekusi pada setiap bulan.
- Hari dalam minggu (*): Eksekusi dapat dilakukan kapanapun.
